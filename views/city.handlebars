<div class="container">
    <div class="row">
        {{#each imageArray}}
        <div class="col-md-6">
            <div class="thumbnail">
                <img src="/images/{{../city}}/{{../city}}-{{this}}.jpeg"/>
            </div>
        </div>
        {{/each}}
    </div>
</div>